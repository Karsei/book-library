buildscript {
    ext {
        set('projectVersion', '0.0.1')
        set('springBootVersion', '2.7.4')
        set('springCloudVersion', '2021.0.4')
        set('springDependencyVersion', '1.0.14.RELEASE')
    }
    repositories {
        mavenCentral()
    }
}